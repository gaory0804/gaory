# 1. 브라우저와 서버

### 🌐 브라우저 (Web Browser)
인터넷에서 웹페이지, 이미지, 영상 등의 콘텐츠를 검색하고 화면에 표시해 주는 응용 프로그램입니다.

### 🖥️ 서버 (Server)
네트워크를 통해 다른 기기(클라이언트)에 데이터, 콘텐츠, 서비스 등의 리소스를 제공하는 컴퓨터나 소프트웨어입니다.

> **💡 브라우저와 서버가 같이 묶이는 이유**
> 한쪽만 있어서는 아무 일도 일어나지 않기 때문입니다.

### 🔄 브라우저와 서버의 관계성
인터넷을 한다는 것은 브라우저가 **요청(Request)**하고, 서버가 **응답(Response)**하는 과정의 반복입니다. 
사용자가 브라우저를 통해 요청을 보내면, 서버가 그 요청을 처리해서 데이터를 보내주고, 브라우저는 그 데이터를 받아 우리 눈에 보이는 화면으로 그려줍니다.

---

# 2. GET과 POST

## 📥 1. GET (보여달라고 요청하는 것)
* **개념:** 특정 리소스의 표시를 요청합니다. 오직 데이터를 받기만 합니다.
* **비유:** 식당에 들어가서 벽에 붙은 메뉴판을 보는 것과 같습니다.
  * *행동:* 메뉴판 읽기 ➡️ *결과:* 메뉴 정보를 얻음.
  * 단, 메뉴판에 적힌 가격을 마음대로 고치거나 메뉴를 새로 추가할 수는 없습니다.

### 🔑 핵심 특징
* 오직 서버에 있는 정보를 가져오기 위해 사용됩니다.
* 서버에 저장된 원래 데이터가 변하거나 삭제될 위험이 없습니다. (**안전함**)
* **주소창 노출:** 브라우저 주소창에 URL을 치고 엔터를 누르는 행위 자체가 GET 요청입니다. 검색창에 검색어를 입력했을 때 주소 뒤에 `?q=검색어`처럼 데이터가 붙어서 보이는 것도 GET 방식의 특징입니다.
* **⚠️ 주의 (실무 권장사항):** HTTP 규칙상 GET 요청에 데이터를 본문(Body)에 담아 보내면 안 된다고 딱 잘라 말하지는 않지만, **실무에서는 절대 하지 말라고 권장**합니다. 서버가 제멋대로 처리하여 에러가 나거나 데이터를 버릴 수 있어 예측 불가능하기 때문입니다. (시한폭탄 ㄷㄷ)

---

## 📤 2. POST (저장/변경해 달라고 제출하는 것)
* **개념:** 특정 리소스에 엔티티(Entity)를 제출할 때 쓰입니다. 이는 종종 서버의 상태 변화나 부작용을 일으킵니다.
* **💡 엔티티(Entity)란?** 서버로 전송하는 실제 데이터입니다. 포도송이에 비유할 수 있습니다.
  * *포도알 하나하나:* 실제 데이터
  * *포도송이 전체를 묶는 이름:* 엔티티
* **특징:** URL에 데이터를 붙여서 보내는 GET과 달리, URL에 노출하지 않고 **BODY(택배 상자 속)에다가 데이터를 넣어서** 보냅니다. 데이터 전송을 기반으로 한 메서드입니다.

---

## 📊 3. GET과 POST 차이점 총정리

| 구분 | 📥 GET | 📤 POST |
| :--- | :--- | :--- |
| **핵심 목적** | 서버에서 데이터 조회 (가져오기) | 서버에 데이터 추가 및 변경 |
| **데이터 배달 위치** | 주소창 (URL 뒤에 매달아 보내기) | 택배 상자 (HTTP Body 안에 숨기기) |
| **보안성** | 주소창에 다 보여서 **낮음** | 주소창에 안 보여서 **비교적 높음** |
| **데이터 크기 제한** | 주소창 길이 제한이 있어서 **작음** | 제한이 없어서 **대용량 가능** |
| **캐시 (창고 보관)** | 신선해서 **가능** | 유통기한 정보가 있을 때만 가능 |
| **속도** | 상대적으로 **빠름** | 상대적으로 **느림** |
| **기타 기능** | 공유 및 북마크(즐겨찾기) 가능 | 새로고침 하면 중복 요청 방지 경고 뜸 |

---

## 🔒 4. 알아두면 좋은 상식

### 🔑 POST도 완전히 안전한 것은 아니다!
누가 중간에서 HTTP Body를 가로채서 열어보면 내용물이 다 보입니다. 그래서 이 HTTP Body 자체를 암호화해서 자물쇠를 채우는 기술이 바로 주소창의 자물쇠 표시인 **HTTPS**입니다.

### 🔄 멱등성 (Idempotency)
동일한 요청을 한 번 보내는 것과 여러 번 연속으로 보내는 것이 같은 효과를 지니고, 서버의 상태도 동일하게 남는 성질을 말합니다.
* **GET:** 여러 번 조회해도 서버의 데이터는 변하지 않으므로 **멱등성을 가집니다.**
* **POST:** 여러 번 요청하면 데이터가 계속 추가되거나 변경되므로 **멱등성을 가지지 않습니다.** (그래서 새로고침 할 때 경고창이 뜹니다.)

### 🍏 신선도 정보 (캐시 유효 기간)
원래 데이터를 캐시(창고)에서 꺼내 먹으려면 유통기한(캐시 유효 기간) 정보가 붙어 있어야 안전하게 꺼내 먹을 수 있습니다.